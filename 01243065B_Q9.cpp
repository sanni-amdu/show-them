1. Assignment Operator (=)
 Purpose:

Used to assign a value to a variable.

 Example:
         int x;
         x = 5;
         
         ? Explanation:

The value 5 is stored in the variable x.
This changes the value of x.

 2. Equality Operator (==)
 Purpose:

Used to compare two values and check if they are equal.

 Example:
         int x = 5;
int y = 5;

if (x == y) {
    cout << "x and y are equal" << endl;
}
``

 Explanation:

x == y checks if both variables have the same value.
If they are equal, the condition is true.
 
